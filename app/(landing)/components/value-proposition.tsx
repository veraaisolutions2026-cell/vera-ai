export function ValueProposition() {
  return (
    <section
      aria-label="value-proposition"
      className="flex min-h-[60vh] items-center justify-center border-b border-border"
    >
      <span className="font-mono text-xs text-muted-foreground">
        — ValueProposition —
      </span>
    </section>
  )
}
