export function Hero() {
  return (
    <section
      aria-label="hero"
      className="flex min-h-[80vh] items-center justify-center border-b border-border"
    >
      <span className="font-mono text-xs text-muted-foreground">— Hero —</span>
    </section>
  )
}
